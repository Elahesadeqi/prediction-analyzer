[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"


[project]
name = "prediction-analyzer"
version = "0.1.0"
description = "A Python pipeline for analyzing machine learning predictions"
requires-python = ">=3.10"


[tool.setuptools.packages.find]
where = ["src"]